import { FilterElementPipe } from './filter-element.pipe';

describe('FilterElementPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterElementPipe();
    expect(pipe).toBeTruthy();
  });
});
